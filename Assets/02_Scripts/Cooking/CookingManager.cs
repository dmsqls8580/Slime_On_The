using UnityEngine;

public class CookingManager : MonoBehaviour
{
    [SerializeField] private RecipeDatabase database;

    // 쿠킹패널에서부터 정보를 얻어와 그 정보에 부합하는 database에 해당하는 ItemSO를 반환.
    public void asdf()
    {
        //database.Recipes[0].recipe[0].item.cookableData.tagMap
    }
}
